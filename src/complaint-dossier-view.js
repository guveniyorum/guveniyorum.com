import { AVATARS, CLOSED_STATUSES, esc, statusLabel, formatDate, formatBytes, loadDossier } from './complaint-dossier-api.js';

export function toast(message, error = false) {
  document.querySelector('[data-pcd-toast]')?.remove();
  const node = document.createElement('div'); node.dataset.pcdToast = '';
  node.className = `pcdToast ${error ? 'error' : ''}`; node.textContent = message;
  document.body.appendChild(node); setTimeout(() => node.remove(), 5200);
}
export function ensureDossierStyles() {
  if (document.getElementById('complaint-public-dossier-style')) return;
  const style = document.createElement('style'); style.id = 'complaint-public-dossier-style'; style.textContent = `
  .pcdToast{position:fixed;right:18px;bottom:18px;z-index:17000;max-width:min(460px,calc(100vw - 36px));padding:13px 15px;border:1px solid rgba(37,240,132,.34);border-radius:14px;background:rgba(8,19,33,.98);color:#d9ffe7;box-shadow:0 18px 60px rgba(0,0,0,.4);font:850 13px/1.45 Inter,system-ui,sans-serif}.pcdToast.error{border-color:rgba(255,77,109,.36);color:#ffc2cb}.pcdFeed{display:grid;gap:10px}.pcdCard{cursor:pointer;transition:.18s ease}.pcdCard:hover{transform:translateY(-1px);border-color:rgba(37,240,132,.34)!important}.pcdCard:focus{outline:2px solid rgba(37,240,132,.45);outline-offset:2px}.pcdAuthor{display:inline-flex;align-items:center;gap:7px}.pcdMiniAvatar{width:25px;height:25px;display:grid;place-items:center;border-radius:9px;background:linear-gradient(135deg,#25f084,#8b5cf6);color:#04120b;font-weight:1000}.pcdEmpty{padding:22px;border:1px dashed rgba(178,204,255,.17);border-radius:16px;text-align:center;color:#91a4bc;background:rgba(255,255,255,.02)}.pcdModal{position:fixed;inset:0;z-index:16500;display:grid;place-items:center;padding:18px;background:rgba(1,4,8,.86);backdrop-filter:blur(16px)}.pcdModalCard{width:min(1120px,100%);max-height:94vh;overflow:auto;border:1px solid rgba(178,204,255,.15);border-radius:24px;background:#07101d;color:#eef7ff;box-shadow:0 32px 110px rgba(0,0,0,.58)}.pcdHead{position:sticky;top:0;z-index:5;display:flex;justify-content:space-between;align-items:center;gap:12px;padding:16px 18px;border-bottom:1px solid rgba(178,204,255,.12);background:rgba(7,16,29,.95)}.pcdBtn{border:1px solid rgba(178,204,255,.15);border-radius:12px;background:rgba(255,255,255,.045);color:#eef7ff;padding:10px 13px;font-weight:850;cursor:pointer}.pcdBtn.primary{border-color:rgba(37,240,132,.36);background:rgba(37,240,132,.12);color:#d8ffe6}.pcdBody{padding:18px;display:grid;gap:15px}.pcdHero{display:grid;grid-template-columns:1.3fr .7fr;gap:14px}.pcdCase,.pcdProfile,.pcdPanel{border:1px solid rgba(178,204,255,.12);border-radius:20px;background:linear-gradient(150deg,rgba(13,24,41,.96),rgba(7,15,27,.96));box-shadow:0 20px 60px rgba(0,0,0,.24)}.pcdCase{padding:21px}.pcdCase h1{margin:10px 0;font-size:clamp(28px,4vw,48px);line-height:1;letter-spacing:-.05em}.pcdCase p{color:#a7b8ca;line-height:1.65}.pcdPills{display:flex;gap:7px;flex-wrap:wrap}.pcdPill{padding:5px 8px;border:1px solid rgba(34,211,238,.24);border-radius:999px;background:rgba(34,211,238,.08);color:#c9f7ff;font-size:10px;font-weight:900}.pcdPill.green{border-color:rgba(37,240,132,.28);background:rgba(37,240,132,.09);color:#caffdc}.pcdProfile{padding:18px}.pcdIdentity{display:flex;align-items:center;gap:12px}.pcdAvatar{width:58px;height:58px;display:grid;place-items:center;border-radius:18px;background:linear-gradient(135deg,#25f084,#8b5cf6);color:#04120b;font-size:27px;font-weight:1000}.pcdIdentity b{display:block;font-size:17px}.pcdIdentity small{display:block;color:#8fa3ba;margin-top:3px}.pcdMetrics{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;margin-top:15px}.pcdMetric{padding:10px;border:1px solid rgba(178,204,255,.10);border-radius:12px;background:rgba(255,255,255,.03)}.pcdMetric b{display:block;color:#9fffc0}.pcdMetric small{color:#8094ab;font-size:10px}.pcdPanel{padding:18px}.pcdGallery{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px}.pcdAsset{overflow:hidden;border:1px solid rgba(178,204,255,.12);border-radius:17px;background:#081321}.pcdMedia{height:280px;display:grid;place-items:center;background:#02060b}.pcdMedia img,.pcdMedia video,.pcdMedia iframe{width:100%;height:100%;object-fit:contain;border:0}.pcdInfo{padding:12px}.pcdInfo small{display:block;color:#8296ad;margin-top:4px}.pcdTimelineRow{display:grid;grid-template-columns:12px 1fr;gap:10px;padding:10px 0;border-bottom:1px solid rgba(178,204,255,.08)}.pcdDot{width:10px;height:10px;margin-top:5px;border-radius:999px;background:#25f084;box-shadow:0 0 15px rgba(37,240,132,.55)}.pcdTimelineRow small{display:block;color:#8296ad;margin-top:3px}.pcdLoading{padding:38px;text-align:center;color:#91a4bc}@media(max-width:860px){.pcdHero,.pcdGallery{grid-template-columns:1fr}.pcdModal{padding:8px}.pcdBody{padding:12px}.pcdMedia{height:230px}}`;
  document.head.appendChild(style);
}
export function publicCard(row) {
  const glyph = AVATARS[row.author_avatar_key] || '◎';
  return `<article class="complaint pcdCard" tabindex="0" data-pcd-public-id="${esc(row.public_id)}"><div class="complaintHead"><div><b>${esc(row.title)}</b><br><small class="muted">${esc(row.public_id)} · ${esc(row.brand_name || 'Bilinmeyen Marka')} · ${esc(row.category || 'Genel')}</small></div><span class="status ${CLOSED_STATUSES.has(row.status) ? 'solved' : ''}">${esc(statusLabel(row.status))}</span></div><p>${esc(row.public_summary || '')}</p><div class="complaintFoot"><span class="pcdAuthor"><span class="pcdMiniAvatar">${esc(glyph)}</span><small class="muted">${esc(row.author_nickname || 'Topluluk Üyesi')} · ${esc(formatDate(row.published_at))}</small></span><span><span class="complaintEvidenceChip">📎 ${Number(row.attachment_count || 0)} kamusal kanıt</span> <button type="button" class="btn green" data-pcd-open="${esc(row.public_id)}">Dosyayı Aç</button></span></div></article>`;
}
function media(item) {
  if (!item.url) return '<div class="pcdLoading">Dosya görüntülenemedi.</div>';
  const url = esc(item.url);
  if (String(item.mime_type).startsWith('image/')) return `<img src="${url}" alt="${esc(item.caption || item.file_name || 'Kanıt')}">`;
  if (String(item.mime_type).startsWith('video/')) return `<video src="${url}" controls playsinline preload="metadata"></video>`;
  if (item.mime_type === 'application/pdf') return `<iframe src="${url}" title="Belge"></iframe>`;
  return `<a class="pcdBtn primary" href="${url}" target="_blank" rel="noopener">Dosyayı Aç</a>`;
}
function renderModal(dossier) {
  document.querySelector('[data-pcd-modal]')?.remove();
  const c = dossier.case || {}, a = dossier.author || {}, glyph = AVATARS[a.avatar_key] || '◎';
  const summary = c.summary || c.public_summary || c.description || '';
  const modal = document.createElement('div'); modal.className = 'pcdModal'; modal.dataset.pcdModal = '';
  modal.innerHTML = `<div class="pcdModalCard"><div class="pcdHead"><div><b>${esc(c.public_id)} · ${esc(statusLabel(c.status))}</b><br><small class="muted">${dossier.access === 'public' ? 'Kamusal dosya görünümü' : 'Yetkili dosya görünümü'}</small></div><button class="pcdBtn" data-pcd-close>Kapat</button></div><div class="pcdBody"><div class="pcdHero"><section class="pcdCase"><div class="pcdPills"><span class="pcdPill green">${esc(c.brand_name || 'Bilinmeyen Marka')}</span><span class="pcdPill">${esc(c.category || 'Genel')}</span><span class="pcdPill">${esc(formatDate(c.created_at || c.published_at))}</span></div><h1>${esc(c.title)}</h1><p>${esc(summary)}</p>${c.resolution_summary ? `<div class="pcdPanel"><b>Çözüm Özeti</b><p>${esc(c.resolution_summary)}</p></div>` : ''}</section><aside class="pcdProfile"><div class="pcdIdentity"><div class="pcdAvatar">${esc(glyph)}</div><div><b>${esc(a.nickname || 'Topluluk Üyesi')}</b><small>Takma adlı topluluk profili</small></div></div><div class="pcdMetrics"><div class="pcdMetric"><b>${Number(a.trust_score || 70)}</b><small>GÜVEN</small></div><div class="pcdMetric"><b>${Number(a.contribution_score || 0)}</b><small>KATKI</small></div><div class="pcdMetric"><b>${Number(a.complaint_count || 0)}</b><small>DOSYA</small></div></div><p class="muted" style="margin-top:12px">Gerçek ad ve e-posta kamusal görünümde paylaşılmaz.</p></aside></div><section class="pcdPanel"><h3>Kanıt Galerisi</h3>${dossier.attachments.length ? `<div class="pcdGallery">${dossier.attachments.map((item, i) => `<article class="pcdAsset"><div class="pcdMedia">${media(item)}</div><div class="pcdInfo"><b>${esc(item.caption || item.file_name || `Kanıt ${i + 1}`)}</b><small>${esc(item.mime_type || item.media_kind || 'Dosya')}${item.file_size ? ` · ${esc(formatBytes(item.file_size))}` : ''}</small></div></article>`).join('')}</div>` : '<div class="pcdEmpty">Bu dosyada kamusal olarak yayımlanmış kanıt yok. Özel kanıtlar yalnızca dosya sahibi ve yetkili taraflara görünür.</div>'}</section><section class="pcdPanel"><h3>Dosya Geçmişi</h3>${dossier.history.length ? dossier.history.map((entry) => `<div class="pcdTimelineRow"><span class="pcdDot"></span><div><b>${esc(statusLabel(entry.status || entry.to_status))}</b><small>${esc(entry.actor_role || 'system')} · ${esc(formatDate(entry.created_at))}${entry.note ? ` · ${esc(entry.note)}` : ''}</small></div></div>`).join('') : '<div class="pcdEmpty">Henüz durum hareketi yok.</div>'}</section></div></div>`;
  document.body.appendChild(modal);
}
export async function openDossier(publicId) {
  if (!publicId) return;
  ensureDossierStyles();
  document.querySelector('[data-pcd-modal]')?.remove();
  const loading = document.createElement('div'); loading.className = 'pcdModal'; loading.dataset.pcdModal = '';
  loading.innerHTML = '<div class="pcdModalCard"><div class="pcdLoading">Şikayet dosyası, profil ve kanıtlar hazırlanıyor…</div></div>'; document.body.appendChild(loading);
  try { const dossier = await loadDossier(publicId); if (!dossier) throw new Error('Bu dosya kamusal değil veya erişim yetkin yok.'); renderModal(dossier); }
  catch (error) { loading.remove(); toast(error?.message || 'Şikayet dosyası açılamadı.', true); }
}
